(def nil (fn () ()))

(def + (fn (x y)
       (add x y)))

(def - (fn (x y)
       (sub x y)))

(def * (fn (x y)
       (mult x y)))

(def / (fn (x y)
       (div x y)))

(def % (fn (x y)
       (mod x y)))

(def ! (fn (n)
       (not n)))
